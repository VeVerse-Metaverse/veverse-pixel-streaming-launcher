package config

// Supported build configurations.
const (
	Development = "Development"
	Test        = "Test"
	Shipping    = "Shipping"
)

const (
	TempDir     = ".tmp"
	DownloadDir = "downloads"
	AppDir      = "apps"
)
